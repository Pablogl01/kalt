// Maps navStore icon names to Lucide SVG components.
// Shared between SidebarNav and BottomTabBar so the icon set stays in one place.
import { Home, CalendarDays, CircleCheck, ShoppingCart, BarChart3, User } from 'lucide-vue-next'

export const navIcons = {
  'home': Home,
  'calendar': CalendarDays,
  'check-circle': CircleCheck,
  'shopping-cart': ShoppingCart,
  'bar-chart': BarChart3,
  'user': User,
}
